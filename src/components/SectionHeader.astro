---
interface Props {
  eyebrow: string;
  title: string;
  intro?: string;
}
const { eyebrow, title, intro } = Astro.props;
---
<div class="section-head">
  <span class="eyebrow">{eyebrow}</span>
  <h2>{title}</h2>
  {intro && <p>{intro}</p>}
</div>
